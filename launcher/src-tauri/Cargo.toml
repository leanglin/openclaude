[package]
name = "opencat-launcher"
version = "7.0.1"
description = "OpenCat launcher"
authors = ["OpenCat"]
edition = "2021"

[lib]
name = "opencat_launcher_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tauri = { version = "2", features = [] }
ureq = "2"
url = "2"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
