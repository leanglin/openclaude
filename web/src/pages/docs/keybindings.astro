---
import DocsLayout from '../../layouts/DocsLayout.astro'
import { keybindings } from '../../data/keybindings'

const toc = [
  { id: 'defaults', label: '默认快捷键' },
  { id: 'customizing', label: '自定义' },
]
---

<DocsLayout
  title="openclaude 快捷键 — 默认值与自定义"
  description="openclaude TUI 默认键盘快捷键，以及如何通过 keybindings 文件（默认 ~/.openclaude/keybindings.json；可通过 OPENCLAUDE_CONFIG_DIR 覆盖）和 /keybindings 命令自定义。"
  heading="快捷键"
  lede="默认快捷键贴近常见 readline/terminal 肌肉记忆，同时所有绑定都可重设。"
  toc={toc}
>
  <h2 id="defaults">默认快捷键</h2>
  <div class="table-wrap">
    <table>
      <thead>
        <tr>
          <th scope="col">按键</th>
          <th scope="col">动作</th>
          <th scope="col">上下文</th>
        </tr>
      </thead>
      <tbody>
        {keybindings.map(k => (
          <tr>
            <td><kbd>{k.keys}</kbd></td>
            <td>{k.action}</td>
            <td>{k.context}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
  <p>
    部分快捷键依赖终端能力，例如 <kbd>Shift+Enter</kbd> 需要通过
    <code>/terminal-setup</code> 安装绑定；某些 <kbd>Cmd</kbd> 组合键需要支持
    kitty-protocol 的终端。
  </p>

  <h2 id="customizing">自定义</h2>
  <p>
    运行 <code>/keybindings</code> 可以打开（或创建）你的 keybindings 文件
    （默认：<code>~/.openclaude/keybindings.json</code>；可通过
    <code>OPENCLAUDE_CONFIG_DIR</code> 覆盖）。
    用户绑定会叠加在默认值之上，所以只需要声明你要修改的部分。支持
    <code>ctrl+x ctrl+e</code> 这类 chord binding。
  </p>
  <pre><code>{`{
  "bindings": {
    "ctrl+u": "chat:undo",
    "ctrl+x ctrl+k": "chat:killAgents"
  }
}`}</code></pre>
  <p>
    在会话中也可以直接问 agent：内置
    <a href="/docs/skills/">keybindings-help skill</a> 了解 schema，并能替你编辑文件。
  </p>
</DocsLayout>
