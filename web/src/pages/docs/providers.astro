---
import DocsLayout from '../../layouts/DocsLayout.astro'
import { providers } from '../../data/providers'

const toc = [
  { id: 'how-it-works', label: 'Provider 设置方式' },
  { id: 'supported-providers', label: '支持的 provider' },
  { id: 'env-setup', label: '环境变量设置' },
]
---

<DocsLayout
  title="openclaude Provider — OpenAI、Gemini、Ollama 与 13+ 后端"
  description="将 openclaude 连接到任意 model provider：Anthropic、OpenAI-compatible APIs、OpenRouter、Gemini、GitHub Models、Codex OAuth、Ollama、LM Studio、Bedrock、Vertex 等。"
  heading="模型 Provider"
  lede="一个 agent，连接所有后端。配置一次 provider，就能在会话中切换模型。"
  toc={toc}
>
  <h2 id="how-it-works">Provider 设置方式</h2>
  <p>
    在会话中运行 <code>/provider</code> 进入引导式设置。它会带你完成 auth、
    选择模型，并把结果保存为 profile，让后续会话和仓库的其他 clone 都能以同样方式启动。
    你也可以通过<a href="/docs/configuration/">环境变量</a>配置所有内容，这对 CI
    和脚本化 <code>--print</code> 运行很方便。
  </p>
  <p>
    可随时用 <code>/model</code> 切换模型，也可以在命令行上传入
    <code>--provider</code> / <code>--model</code>；详见
    <a href="/docs/cli-reference/">CLI 参考</a>。
  </p>

  <h2 id="supported-providers">支持的 provider</h2>
  <div class="table-wrap">
    <table>
      <thead>
        <tr>
          <th scope="col">provider</th>
          <th scope="col">设置方式</th>
          <th scope="col">说明</th>
        </tr>
      </thead>
      <tbody>
        {providers.map(p => (
          <tr id={p.id}>
            <td>{p.name}</td>
            <td>{p.setup}</td>
            <td>
              {p.notes}
              {p.envVars && <span class="env-list"> 使用 <code>{p.envVars.join(', ')}</code></span>}
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>

  <h2 id="env-setup">环境变量设置</h2>
  <p>最快的 OpenAI-compatible 设置（适用于 OpenRouter、DeepSeek、Groq、LM Studio、LiteLLM 和任何 /v1 server）：</p>
  <pre><code>export CLAUDE_CODE_USE_OPENAI=1
export OPENAI_API_KEY=sk-your-key-here
export OPENAI_BASE_URL=https://openrouter.ai/api/v1   # optional, defaults to OpenAI
export OPENAI_MODEL=deepseek/deepseek-chat

openclaude</code></pre>
  <p>
    本地 Ollama 完全不需要 key；在 <code>/provider</code> 中指向它并选择已 pull 的模型即可。
    完整环境变量列表见<a href="/docs/configuration/#environment-variables">配置页</a>。
  </p>
</DocsLayout>
