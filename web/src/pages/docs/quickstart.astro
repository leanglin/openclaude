---
import DocsLayout from '../../layouts/DocsLayout.astro'
import { SITE } from '../../data/site'

const toc = [
  { id: 'start', label: '启动 agent' },
  { id: 'pick-a-provider', label: '选择 provider' },
  { id: 'first-task', label: '交给它一个任务' },
  { id: 'review', label: '审查改动' },
  { id: 'next-steps', label: '下一步' },
]
---

<DocsLayout
  title="openclaude 快速开始 — 5 分钟完成第一个会话"
  description="运行你的第一个 openclaude 会话：在仓库中启动 agent，选择 model provider，交给它一个任务，并审查 diff。从安装到可工作的 agent 只需几分钟。"
  heading="快速开始"
  lede="从全新安装到拿到可审查的 diff，大约五分钟。"
  toc={toc}
>
  <h2 id="start">1. 启动 agent</h2>
  <p>在任意仓库（或空目录）中运行：</p>
  <pre><code>cd your-repo
openclaude</code></pre>
  <p>
    如果还没有<a href="/docs/installation/">安装</a>：
    <code>{SITE.installCommand}</code>
  </p>

  <h2 id="pick-a-provider">2. 选择 provider</h2>
  <p>
    在会话中输入 <code>/provider</code> 进入引导式设置，并保存 profile。
    它可以接入 OpenAI、OpenRouter、Gemini、本地 Ollama 以及十多个后端；
    完整列表见 <a href="/docs/providers/">provider 指南</a>。
  </p>
  <p>更喜欢环境变量？最快的 OpenAI-compatible 配置如下：</p>
  <pre><code>export CLAUDE_CODE_USE_OPENAI=1
export OPENAI_API_KEY=sk-your-key-here
export OPENAI_MODEL=gpt-4o

openclaude</code></pre>
  <p>使用 GitHub Models 时，运行 <code>/onboard-github</code> 进入交互式 OAuth onboarding。</p>

  <h2 id="first-task">3. 交给它一个任务</h2>
  <p>像给队友交代任务一样写 prompt：</p>
  <pre><code>&gt; 给 fetch client 添加 exponential backoff 重试，然后运行测试</code></pre>
  <p>
    agent 会实时流式展示计划、工具调用和编辑：读取文件、搜索、编辑、运行命令。
    工具使用受权限控制；敏感工具首次使用需要你批准，后续可用 <code>/permissions</code> 管理规则。
  </p>

  <h2 id="review">4. 审查改动</h2>
  <ul>
    <li><code>/diff</code>：查看未提交改动和每轮 diff。</li>
    <li><code>/rewind</code>：把代码和/或对话恢复到之前的某个点。</li>
    <li><code>/cost</code>：查看当前会话到目前为止的成本。</li>
  </ul>
  <p>除非你明确要求，否则不会提交任何内容；每次改动都保持可审查。</p>

  <h2 id="next-steps">下一步</h2>
  <ul>
    <li>浏览 <a href="/docs/slash-commands/">斜杠命令参考</a>：会话、上下文、审查等。</li>
    <li>脚本化使用：<code>openclaude -p "explain this repo"</code>，详见 <a href="/docs/cli-reference/">CLI 参考</a>。</li>
    <li>按你的习惯定制：<a href="/docs/configuration/">配置</a>和 <a href="/docs/keybindings/">快捷键</a>。</li>
  </ul>
</DocsLayout>
